# 语音支付功能集成指南

---

## 📦 文件清单

本次提交的语音支付模块包含以下文件：

### 后端代码（C++）

```
code/
├── voice_recognition.h           # 语音识别封装类
├── voice_payment_service.h       # 语音支付服务层
└── (修改) ../service/payment.h   # 在原有文件中新增VoicePay接口
```

### 前端代码（微信小程序）

```
code/
├── voice_payment.wxml            # 页面结构
├── voice_payment.wxss            # 样式文件
├── voice_payment.js              # 页面逻辑
└── voice_payment.json            # 页面配置
```

### 文档

```
code/
├── README.md                     # 功能说明文档
├── API文档.md                    # 接口文档
└── 集成指南.md                   # 本文档
```

---

## 🔧 后端集成步骤

### Step 1: 复制头文件到项目

将语音支付相关头文件复制到项目目录：

```bash
# 在项目根目录执行
cd C:\Users\28614\Desktop\作业\软工

# 创建语音支付模块目录（如果还没创建）
mkdir -p service/voice

# 复制头文件
cp code/voice_recognition.h service/voice/
cp code/voice_payment_service.h service/voice/
```

### Step 2: 修改现有 payment.h

已在 `service/payment.h` 中添加了以下接口（第131-154行）：

```cpp
/**
 * 语音支付接口
 */
static VoicePaymentResponse VoicePay(
    const std::string& user_id, 
    const std::string& audio_data,
    const std::string& session_id = "") {
    
    return VoicePaymentService::ProcessVoicePayment(user_id, audio_data, session_id);
}

/**
 * 语音查询待缴费项目
 */
static std::string VoiceQueryUnpaidItems(const std::string& user_id) {
    return VoicePaymentService::QueryUnpaidItemsByVoice(user_id);
}
```

### Step 3: 在 payment.h 中添加头文件引用

在 `service/payment.h` 文件开头添加：

```cpp
#include "voice/voice_recognition.h"
#include "voice/voice_payment_service.h"
```

### Step 4: 配置语音识别API密钥

在 `config/app.ini` 中添加：

```ini
[baidu_voice]
api_key = YOUR_BAIDU_API_KEY
secret_key = YOUR_BAIDU_SECRET_KEY
app_id = YOUR_APP_ID
```

**获取密钥步骤：**

1. 访问 https://ai.baidu.com/
2. 注册账号 → 创建应用 → 选择"语音识别"
3. 复制 API Key、Secret Key 和 App ID
4. 粘贴到配置文件

### Step 5: 添加HTTP路由

在 `controller/payment_controller.cpp` 中添加路由处理：

```cpp
#include "service/payment.h"

// 语音支付路由
void PaymentController::VoicePayHandler(const HttpRequest& req, HttpResponse& res) {
    try {
        // 解析请求参数
        auto json_data = JsonUtil::Parse(req.body);
        std::string user_id = json_data.value("user_id", "");
        std::string audio_data = json_data.value("audio_data", "");
        std::string session_id = json_data.value("session_id", "");

        // 调用服务层
        auto response = PaymentService::VoicePay(user_id, audio_data, session_id);

        // 返回JSON响应
        res.SetStatus(200);
        res.SetHeader("Content-Type", "application/json");
        res.SetBody(JsonUtil::Serialize(response));

    } catch (const std::exception& e) {
        SPDLOG_ERROR("Voice pay handler error: {}", e.what());
        res.SetStatus(500);
        res.SetBody(R"({"success": false, "error": "Internal server error"})");
    }
}

// 语音查询路由
void PaymentController::VoiceQueryHandler(const HttpRequest& req, HttpResponse& res) {
    try {
        auto json_data = JsonUtil::Parse(req.body);
        std::string user_id = json_data.value("user_id", "");

        std::string reply = PaymentService::VoiceQueryUnpaidItems(user_id);

        nlohmann::json response = {
            {"success", true},
            {"reply_text", reply}
        };

        res.SetStatus(200);
        res.SetHeader("Content-Type", "application/json");
        res.SetBody(response.dump());

    } catch (const std::exception& e) {
        SPDLOG_ERROR("Voice query handler error: {}", e.what());
        res.SetStatus(500);
        res.SetBody(R"({"success": false, "error": "Internal server error"})");
    }
}
```

在路由注册中添加：

```cpp
// main.cpp 或路由配置文件
router.Post("/api/payment/voice-pay", PaymentController::VoicePayHandler);
router.Post("/api/payment/voice-query-unpaid", PaymentController::VoiceQueryHandler);
```

### Step 6: 编译测试

```bash
cd service
mkdir -p build && cd build
cmake ..
make

# 运行测试
./payment_service_test
```

---

## 📱 前端集成步骤

### Step 1: 创建页面目录

```bash
cd C:\Users\28614\Desktop\作业\软工\minicode-1\pages
mkdir -p voice_payment
```

### Step 2: 复制前端文件

```bash
# 在 minicode-1/pages/voice_payment 目录下
cp ../../shihanlin_code/voice_payment.* ./
```

文件清单：
- `voice_payment.wxml` - 页面结构
- `voice_payment.wxss` - 样式
- `voice_payment.js` - 逻辑
- `voice_payment.json` - 配置

### Step 3: 注册页面路由

在 `minicode-1/app.json` 中添加：

```json
{
  "pages": [
    "pages/index/index",
    "pages/Living_payment/Living_payment",
    "pages/Hospital_registration/Hospital_registration",
    "pages/Voice_taxi/Voice_taxi",
    "pages/Olds_chatting/Olds_chatting",
    "pages/voice_payment/voice_payment"  ← 新增
  ]
}
```

### Step 4: 配置微信同声传译插件

在 `minicode-1/app.json` 中添加插件声明：

```json
{
  "pages": [...],
  "plugins": {
    "WechatSI": {
      "version": "0.3.5",
      "provider": "wx069ba97219f66d99"
    }
  }
}
```

### Step 5: 添加首页入口

在 `pages/index/index.wxml` 中添加语音支付入口：

```xml
<!-- 功能卡片 -->
<view class="function-grid">
  <!-- 原有功能 -->
  <navigator url="/pages/Voice_taxi/Voice_taxi" class="function-item">
    <image src="/images/taxi.png" class="function-icon"/>
    <text class="function-text">语音打车</text>
  </navigator>
  
  <!-- 新增：语音支付 -->
  <navigator url="/pages/voice_payment/voice_payment" class="function-item">
    <image src="/images/voice-pay.png" class="function-icon"/>
    <text class="function-text">语音缴费</text>
  </navigator>
  
  <!-- 其他功能... -->
</view>
```

### Step 6: 准备图标资源

将以下图标放入 `minicode-1/images/` 目录：

```
images/
├── voice-pay.png          # 首页入口图标
├── mic-idle.png           # 麦克风待机状态
├── mic-recording.gif      # 录音中动画
├── mic-primary.png        # 按钮麦克风图标（主色）
├── mic-white.png          # 按钮麦克风图标（白色）
├── loading.gif            # 加载动画
├── success.png            # 成功图标
├── error.png              # 错误图标
└── speaker.gif            # 播放中图标
```

**图标设计要求：**
- 尺寸：128x128px（@2x: 256x256px）
- 格式：PNG（透明背景）
- 风格：简洁、扁平化、适老化（高对比度）

### Step 7: 修改API地址

在 `voice_payment.js` 中，全局替换API地址：

```javascript
// 将所有 https://your-api.com 替换为实际后端地址
const API_BASE_URL = 'https://your-actual-api.com';

// 示例
wx.request({
  url: `${API_BASE_URL}/api/payment/voice-pay`,
  // ...
});
```

### Step 8: 配置服务器域名

在微信公众平台 → 小程序管理 → 开发 → 开发设置 → 服务器域名中添加：

- **request合法域名**: `https://your-actual-api.com`
- **uploadFile合法域名**: `https://your-actual-api.com`
- **downloadFile合法域名**: `https://your-actual-api.com`

### Step 9: 申请权限

在微信公众平台 → 小程序管理 → 开发 → 接口设置中开启：

- ✅ **录音功能**（scope.record）
- ✅ **网络请求**

在 `minicode-1/app.json` 中声明权限：

```json
{
  "permission": {
    "scope.record": {
      "desc": "您的麦克风权限将用于语音支付功能，帮助您通过说话完成缴费"
    }
  }
}
```

---

## 🧪 测试验证

### 后端测试

#### 1. 单元测试

创建 `service/test/voice_payment_test.cpp`：

```cpp
#include <gtest/gtest.h>
#include "service/payment.h"

TEST(VoicePaymentTest, ExtractPaymentIntent) {
    auto intent = VoiceRecognition::ExtractPaymentIntent("我要缴水费");
    EXPECT_TRUE(intent.is_payment);
    EXPECT_EQ(intent.payment_type, "水费");
}

TEST(VoicePaymentTest, ProcessVoicePayment) {
    // 模拟音频数据
    std::string mock_audio = "base64_mock_data";
    
    auto response = PaymentService::VoicePay("USER123", mock_audio, "");
    EXPECT_TRUE(response.success || !response.success); // 至少不崩溃
}
```

运行测试：

```bash
cd service/build
./run_tests
```

#### 2. API测试（使用Postman）

**测试用例1：语音支付（首次交互）**

```
POST https://your-api.com/api/payment/voice-pay
Content-Type: application/json

{
  "user_id": "USER123",
  "audio_data": "模拟base64数据",
  "session_id": ""
}

预期响应：
{
  "success": true,
  "reply_text": "...",
  "session_id": "VOICE_PAY_SESSION...",
  "next_action": "continue"
}
```

### 前端测试

#### 1. 开发者工具测试

1. 打开微信开发者工具
2. 导入项目 `minicode-1`
3. 进入语音支付页面
4. 点击"按住说话"按钮
5. 检查控制台日志

#### 2. 真机测试

1. 点击"预览"生成二维码
2. 用微信扫码打开小程序
3. 测试语音录音和识别
4. 验证TTS播报

#### 3. 测试检查清单

- [ ] 录音按钮可以正常按住/松开
- [ ] 录音时有震动反馈
- [ ] 录音后能正常上传到后端
- [ ] 后端返回的文本能正常显示
- [ ] TTS语音播报正常
- [ ] 多轮对话状态正常切换
- [ ] 支付流程完整可用

---

## 🐛 常见问题排查

### 问题1：录音权限被拒绝

**现象**: 点击按钮后无反应，控制台提示权限错误

**解决方案**:
1. 检查 `app.json` 中是否声明了 `scope.record`
2. 引导用户在设置中开启麦克风权限
3. 真机测试时确保用户点击了"允许"

### 问题2：语音识别失败

**现象**: 返回"没有听清您说的话"

**可能原因**:
1. 音频格式不正确（检查编码格式）
2. 音频数据为空（检查base64编码）
3. 百度API密钥错误（检查配置文件）
4. 网络问题（检查API连通性）

**排查步骤**:
```bash
# 1. 检查配置文件
cat config/app.ini | grep baidu

# 2. 测试API连通性
curl -X POST "https://vop.baidu.com/server_api" \
  -H "Content-Type: application/json" \
  -d '{"format":"pcm","rate":16000,...}'

# 3. 检查日志
tail -f logs/voice_payment.log
```

### 问题3：TTS播报无声音

**现象**: 文字显示正常，但没有语音播报

**解决方案**:
1. 检查微信同声传译插件是否正确引入
2. 检查手机音量是否打开
3. 降级为后端TTS接口
4. 使用文字提示兜底

### 问题4：会话过期

**现象**: 多轮对话时提示"会话已过期"

**解决方案**:
1. 检查会话有效期设置（默认3分钟）
2. 前端正确保存并传递 `session_id`
3. 后端检查会话缓存是否正常

### 问题5：编译错误

**现象**: C++代码编译失败

**解决方案**:
```bash
# 检查依赖库
pkg-config --libs libcurl
pkg-config --libs fmt

# 安装缺失依赖
# Ubuntu/Debian
sudo apt-get install libcurl4-openssl-dev libfmt-dev

# macOS
brew install curl fmt

# 重新编译
cd service/build
cmake .. && make
```

---

## 📊 性能优化建议

### 后端优化

1. **语音识别缓存**：相同音频避免重复识别
2. **会话管理**：使用Redis存储会话，提升并发能力
3. **异步处理**：语音识别采用异步方式，不阻塞主线程
4. **连接池**：HTTP客户端使用连接池，减少连接开销

### 前端优化

1. **音频压缩**：上传前对音频进行压缩（降低采样率/比特率）
2. **防抖处理**：避免用户频繁触发录音
3. **离线缓存**：常用语音回复本地缓存
4. **预加载**：提前加载TTS插件

---

## 🚀 上线检查清单

### 后端上线

- [ ] 配置文件已更新（API密钥、数据库连接等）
- [ ] 日志级别设置为INFO（生产环境）
- [ ] 错误监控已接入（Sentry/Bugsnag）
- [ ] 性能监控已接入（Prometheus/Grafana）
- [ ] 负载均衡已配置
- [ ] HTTPS证书已配置
- [ ] 防火墙规则已设置
- [ ] 备份策略已制定

### 前端上线

- [ ] 所有API地址已替换为生产环境
- [ ] 图标资源已准备齐全
- [ ] 隐私政策已添加
- [ ] 用户协议已添加
- [ ] 微信服务器域名已配置
- [ ] 小程序已提交审核
- [ ] 体验版已测试通过
- [ ] 灰度发布计划已制定

---
